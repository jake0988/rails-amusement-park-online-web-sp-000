<%= form_for(@user) do |f| %>
<%= f.collection_select :name, User.all, :id, :name %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :admin %>
<%= f.check_box :admin %>
<%= f.label :password %>
<%= f.text_field :password %>
<%= f.submit "Sign In"%>
<% end %>